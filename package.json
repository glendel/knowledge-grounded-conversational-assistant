{
  "name": "knowledge-grounded-conversational-assistant",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable white-label Core for knowledge-grounded conversational assistants.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "check:boundary": "node ./tools/verify-core-boundary.js",
    "lint": "node ./tools/lint-core.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:security": "node --test tests/security/*.test.js",
    "test": "npm run test:unit && npm run test:security",
    "check": "npm run check:boundary && npm run lint && npm run test"
  }
}
